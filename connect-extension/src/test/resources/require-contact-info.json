{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {
    "contact.email": {
      "type": "string",
      "description": "team email address to be contacted when the connector is affected by maintenance work on kafka connect"
    },
    "contact.teams": {
      "type": "string",
      "description": "url to the ms teams channel to be contacted when the connector is affected by maintenance work on kafka connect"
    }
  },
  "required": [
    "contact.email",
    "contact.teams"
  ]
}
